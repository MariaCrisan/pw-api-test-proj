# Kafka tests

Run with `npm run test:kafka`. Set `RUN_INTEGRATION_TESTS=true` after starting Docker services to include real broker tests.

Use unique consumer groups, correlation IDs, explicit timeouts, and schema assertions for every consumed message.
