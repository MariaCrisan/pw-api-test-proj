# Negative tests

Run with `npm run test:negative`.

Cover invalid payloads, missing fields, authentication failures, and malformed messages. Assert the expected failure response rather than only the status code.
